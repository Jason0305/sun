<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.lonton.petstore.mappers.UserMapper">
    <insert id="insert" parameterType="com.lonton.petstore.entity.User">
        INSERT INTO
            t_user (
            id,
            username,
            password,
            gender,
            email,
            phone,
            avatar,
            salt,
            status,
            is_delete,
            created_user,
            created_time)
        VALUE (
            #{id},
            #{username},
            #{password},
            #{gender},
            #{email},
            #{phone},
            #{avatar},
            #{salt},
            #{status},
            #{isDelete},
            #{createdUser},
            #{createdTime})
    </insert>

    <select id="getUserByUsername" resultType="com.lonton.petstore.entity.User">
        SELECT
            id,username,password,gender,email,phone,avatar,salt,status,
            is_delete,created_user,created_time,modified_user,modified_time
        FROM
           t_user
        WHERE
            username = #{username}
    </select>

    <select id="getUserById" parameterType="com.lonton.petstore.entity.User"
            resultType="com.lonton.petstore.entity.User">
        SELECT
            id,username,password,gender,email,phone,avatar,salt,status,
            is_delete,created_user,created_time,modified_user,modified_time
        FROM
           t_user
        WHERE
            id = #{id}
    </select>
</mapper>